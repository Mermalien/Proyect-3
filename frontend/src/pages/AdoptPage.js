import { Link } from "react-router-dom";

export const AdoptPage= () =>{
    return(
        <section>
            <Link to='/adopta'>
            <h1>¿Estás pensando en adoptar?</h1>
            </Link>
        </section>
    )
}